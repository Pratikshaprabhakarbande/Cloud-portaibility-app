# ai/

Amazon Bedrock (Claude) client and prompt templates used by the AI Cloud
Architect, Portability Analyzer, Migration Advisor, Incident Analyzer, and
ChatOps modules.

Includes a deterministic **mock generator** so AI features still demo without
Bedrock credentials (Demo Mode).
