# config/

Environment loading, database connection, and shared constants.

Planned files (Phase 5–6):
- `env.js` — validates & exports typed environment variables.
- `db.js` — Mongoose connection bootstrap with retry.
- `constants.js` — roles, providers, error codes.
