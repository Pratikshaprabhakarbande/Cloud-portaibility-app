# models/

Mongoose schemas for the collections defined in Phase 3:
`users`, `deployments`, `cloud_resources`, `security_reports`, `cost_reports`,
`migration_reports`, `incident_reports`, `ai_recommendations`,
`portability_reports`, `compliance_reports`.
