# routes/

Express route definitions, one file per module. Routes attach middleware
(auth, RBAC, validation) and delegate to controllers.

Planned (Phase 5): `auth.routes.js`, `dashboard.routes.js`, `security.routes.js`,
`finops.routes.js`, `terraform.routes.js`, `ai.routes.js`, etc.
